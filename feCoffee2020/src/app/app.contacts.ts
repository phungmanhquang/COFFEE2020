import {environment as config } from '../environments/environment' ;
export class AppContact {
	public static CART_CURRENT = config.CART_CURRENT;
	public static FIREBASE_CONFIG = config.firebaseConfig;
	public static USER_CURRENT = config.USER_CURRENT;
}